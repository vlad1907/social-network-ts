
export type SidebarType = {}
type SidebarActionTypes = any

let initialState = {}

export const sidebarReducer = (state: SidebarType = initialState, action: SidebarActionTypes) => {

    return state;
}